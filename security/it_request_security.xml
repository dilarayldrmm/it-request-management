<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <!-- ===================================================== -->
    <!-- SECURITY GROUPS -->
    <!-- ===================================================== -->

    <record id="group_it_request_user" model="res.groups">
        <field name="name">IT Request User</field>
    </record>


    <record id="group_it_request_manager" model="res.groups">
        <field name="name">IT Request Manager</field>

        <field
            name="implied_ids"
            eval="[(4, ref('group_it_request_user'))]"
        />
    </record>


    <!-- ===================================================== -->
    <!-- RECORD RULES -->
    <!-- ===================================================== -->


    <!--
        IT REQUEST USER

        Normal kullanıcı sadece:
        - kendisinin oluşturduğu
        - veya kendisine atanmış

        talepleri görebilir.
    -->

    <record id="rule_it_request_user" model="ir.rule">

        <field name="name">
            IT Request User: Own or Assigned Requests
        </field>

        <field
            name="model_id"
            ref="model_it_request"
        />

        <field name="domain_force">
            ['|',
                ('requester_id', '=', user.id),
                ('assigned_to_id', '=', user.id)
            ]
        </field>

        <field
            name="groups"
            eval="[(4, ref('group_it_request_user'))]"
        />

        <field
            name="perm_read"
            eval="True"
        />

        <field
            name="perm_write"
            eval="True"
        />

        <field
            name="perm_create"
            eval="True"
        />

        <field
            name="perm_unlink"
            eval="False"
        />

    </record>


    <!--
        IT REQUEST MANAGER

        Manager bütün IT Request kayıtlarını görebilir
        ve yönetebilir.
    -->

    <record id="rule_it_request_manager" model="ir.rule">

        <field name="name">
            IT Request Manager: All Requests
        </field>

        <field
            name="model_id"
            ref="model_it_request"
        />

        <field name="domain_force">
            [(1, '=', 1)]
        </field>

        <field
            name="groups"
            eval="[(4, ref('group_it_request_manager'))]"
        />

        <field
            name="perm_read"
            eval="True"
        />

        <field
            name="perm_write"
            eval="True"
        />

        <field
            name="perm_create"
            eval="True"
        />

        <field
            name="perm_unlink"
            eval="True"
        />

    </record>

</odoo>